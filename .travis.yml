sudo: false
language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

install:
  - pip install python-coveralls
  - pip install pytest-pep8
  - pip install pytest-cov

script:
  - export PYTHONPATH=.
  - py.test tests --pep8 rml -v --cov rml

after_success:
  - coveralls
